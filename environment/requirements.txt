# CWRU working-paper reproducibility environment
# Python 3.12.8; install torch from the CPU index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r environment/requirements.txt
# Versions below are the exact ones used to produce results/tables/*.json.
numpy==2.4.0
scipy==1.16.3
scikit-learn==1.8.0
matplotlib==3.10.8
torch==2.9.1
